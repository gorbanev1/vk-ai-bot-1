V188.7 installation

Base required: V188.6.

PATCH/FULL SOURCES:
1. Stop the bot.
2. Back up your existing .env and data/state directories.
3. Extract Gigorave_V1887_full_sources.zip into a clean project directory (or copy the changed source files over V188.6).
4. Keep your existing .env and operational data; do not replace them from an archive.
5. Install dependencies with the project's package manager if needed: pnpm install
6. Run: pnpm dev
7. Confirm startup markers:
   [BOT BUILD] events-v1887-strict-participant-summary-vk-event-evidence
   [BOT PATCH] events-v1887-strict-participant-summary-vk-event-evidence
8. Regression check: npm run test:v1887

Behavior checks:
- `Гигорейв резюмируй` -> usage hint, not last 100 messages.
- `Гигорейв резюмируй Тимасина` -> full linked-history participant summary with own messages + mention contexts.
- Weekly hierarchy report -> Sunday 20:00 only.
- Monthly hierarchy report -> final calendar day 20:00 only.
- VK event page with structured `start_date` should log `[MANUAL EVENT VK STRUCTURED FIRST PASS]`; if those fields are unavailable, generic page parsing continues.
