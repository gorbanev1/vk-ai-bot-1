Gigorave V166 — history recovery / SQLite recovery hardening

- Restored the predecessor VK chat history into the recreated active chat without rolling the database back and losing newer messages.
- Preserved the canonical peer mapping between the old source chat and the active target chat through peer_history_migrations.
- Recovery work was performed on a clean SQLite copy and validated with PRAGMA quick_check before handoff.
- Added safeguards around recovery backups so history-link snapshots remain usable recovery sources.
- Isolated the V132/V133 history tests from the production data/bot.sqlite so synthetic 8xxxxxxxxx peer IDs no longer pollute the live database.
- Release packages keep the operational database separate from source-only overlays unless a recovery database is explicitly shipped.

Historical note: V167–V169 were not published as numbered source releases in this recovery series.
