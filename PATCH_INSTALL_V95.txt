V95 — authoritative date/time + complete-link dedupe афиши

1. Останови бота.
2. Распакуй V95 поверх текущего проекта V94.
3. НЕ удаляй и НЕ заменяй:
   .env
   data/
   SQLite БД
   browser profile
   parsed_secrets.txt
4. Никакой новый парсинг для проверки НЕ нужен: V95 заново читает уже сохранённые raw events перед выдачей.
5. Проверка V95:
   pnpm run test:event-v95-regression
   pnpm run test:event-two-contour
   pnpm run test:event-normalization
   pnpm run test:event-moderation
6. Запуск:
   pnpm dev

Ожидаемая сборка:
[BOT BUILD] events-v95-authoritative-date-time-complete-link-dedupe

Проверка на текущей БД:
Гигорейв тусы август кратко

Ожидаемо:
- TATTOOMO 2026: одна карточка, только 21–23 августа, без 24–31 августа;
- никакого ложного времени 21:08 из даты 21.08.26;
- Мото Што-то!: одна карточка, только 21 и 22 августа с соответствующими временами;
- Двойной Кураж: только 21 августа;
- The Second-to-Last Saturday и Котик остаются разными событиями;
- участники разных концертов не смешиваются;
- если одно событие найдено в TG/VK/беседе, источники перечисляются внутри одной карточки;
- если один ивент реально идёт два дня и площадки разные, выводится отдельная строка расписания для каждого дня.
