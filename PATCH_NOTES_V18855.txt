Gigorave V188.55 — fresh diagnostic boundary + V188.54 parser/model fixes

Build: events-v18855-fresh-runtime-logs
Version: 0.188.55

1. On the FIRST startup of V188.55 only, previously processed volatile diagnostics are deleted:
   - data/logs/
   - data/audit/media/
   - data/ai-token-usage/
   - data/event-ingest-audit.jsonl
   - AI_FULL_AUDIT_RESULTS/
   - GRAPHICS_MATRIX_RESULTS/

2. Databases/history are NEVER part of this cleanup. The allowlist does not include:
   - data/bot.sqlite, WAL/SHM
   - healthy-backups / corruption-backups
   - live-message-journal / vk-message-archive-journal
   - operation-checkpoints
   - LocalAppData state databases

3. Cleanup is one-time per V188.55 installation. A marker is written to
   data/.log-cleanup-v18855.done.json. Normal restarts keep all new logs.
   FORCE_V18855_LOG_CLEANUP=1 may be used only when an explicit second clean boundary is wanted.

4. Includes V188.53 image fix:
   - fixes invalid vision-default resolver alias;
   - finite image ladder with proper escalation and per-image forensic status;
   - recursive reply/repost/forward media context remains enabled.

5. Includes V188.54 parser/model audit fixes:
   - parser AI timeout defaults to 75s instead of 15s;
   - manual parser retry pool is bounded (default 6 cycles / 2 hours);
   - active communication uses cheap-first finite ladder instead of random Luna/Terra entry;
   - active communication AI timeout defaults to 75s.
