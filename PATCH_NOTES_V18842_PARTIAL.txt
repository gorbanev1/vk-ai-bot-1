Gigorave V188.42 PARTIAL / SAFE HOTFIX

DONE:
- Keeps the V188.41 cheapest-to-strongest summary model ladder (Sol/pro3 last).
- hierarchy_message_state is now immutable: ON CONFLICT(message_key) DO NOTHING.
- Already-covered message keys are never sent to summary AI again even if text changes later.
- A normalized sender+timestamp+text content hash suppresses recovered duplicates that arrive under a different archive/source key.
- Recovery duplicate count is logged in HIERARCHY SCAN.

NOT DONE IN THIS PARTIAL BUILD:
- New separate daily / weekly / monthly rollup tables are not wired yet.
- Daily rollup is not yet moved to 00:00 local time.
- Weekly rollup is not yet moved to Sunday 24:00 / Monday 00:00.
- Monthly rollup is not yet moved to the last-day 24:00 boundary.
- Weekly is still not guaranteed to consume only daily summaries.
- Monthly is still not guaranteed to consume only weekly summaries.
- All-time summary still uses the existing hierarchical tree, not the proposed month/week/day non-overlapping rollup set.

This archive is intentionally labelled partial so the unfinished architecture is not presented as complete.
