Gigorave V188.1 — direct GPT chat context + current-author identity

Что исправлено
1. Прямые обращения к боту (обычный GPT route) теперь получают до 40 ПРЕДЫДУЩИХ сообщений всей групповой беседы, а не только последние сообщения самого автора.
2. Каждая реплика контекста подписывается как: Имя [vk:ID]: текст (для Telegram: Имя [tg:ID]: текст).
3. Текущий автор обращения передаётся отдельно по имени + стабильному ID.
4. Для первого лица добавлена явная семантика: «я / мне / меня / мой / моя / моё» в текущем запросе относятся к текущему автору, если текст явно не задаёт другое.
5. Active Communication не изменён: его собственное окно контекста остаётся отдельным.
6. Summary/dossier/astrology не получают этот 40-message chat context автоматически; у них свои специализированные источники данных.

Диагностика
При обычном прямом GPT-запросе появляется строка:
[DIRECT GPT CHAT CONTEXT] peer=... sender=... name="..." messages=40 chars=...

Проверки
- direct-context tests: OK
- import check: OK (328 files)
- named import check: OK (140 source files, missing=0)
- targeted regressions: 21/21 passed
