import fs from 'node:fs';
const p='src/platforms/vk/vkPublicScraper.js';
let s=fs.readFileSync(p,'utf8');
let a=s.indexOf('            const currentDomExactPosts = await extractExactVkPostsFromDomSnapshot(');
let b=s.indexOf("            console.log(\n                '[VK PUBLIC PAGE CAPTURED]'",a);
if(a<0||b<0) throw new Error('final parser block not found');
const block=`            const snapshotPosts = await extractVkPublicPostsFromImmutableSnapshot(\n                page, fullDomHtml, screenName,\n            ).catch((parserError) => {\n                parserReport.exactErrors.push({ at: new Date().toISOString(), message: String(parserError?.message ?? parserError), name: String(parserError?.name ?? '') });\n                diagnostics?.log?.('parser.snapshot.error', { sourceId: \`vk:\${screenName}\`, error: parserError });\n                return [];\n            });\n            const sig = snapshotPosts?.snapshotDiagnostics || {};\n            snapshotExactCount = Number(sig.exactParsed || 0);\n            const parserComparison = {\n                sourceId: \`vk:\${screenName}\`, immutableSnapshot: true,\n                exactCount: Number(sig.exactParsed || 0), structuralCount: Number(sig.structuralParsed || 0), heuristicCount: Number(sig.heuristicParsed || 0),\n                recoveredByStructural: Number(sig.recoveredByStructural || 0), recoveredByHeuristic: Number(sig.recoveredByHeuristic || 0),\n                finalCount: snapshotPosts.length, signature: sig, fallbackUsed: Number(sig.recoveredByStructural || 0) > 0 || Number(sig.recoveredByHeuristic || 0) > 0,\n            };\n            parserReport.comparisons.push(parserComparison);\n            diagnostics?.log?.('parser.compare', parserComparison);\n            for (const post of snapshotPosts.slice(0, targetCount)) collected.set(\`\${post.ownerId}_\${post.postId}\`, post);\n\n`;
s=s.slice(0,a)+block+s.slice(b);
fs.writeFileSync(p,s); console.log('public final immutable 3-pass wired');
