# V188.134: точный маршрут целого TXT без батчей

- `src/features/audit/textAttachmentModelSelection.js`: распознавание `/txt_full` до выбора модели/усилия рассуждения, очистка пользовательского промпта от технического ключа.
- `src/app/botApplication.js`: единый флаг `wholeFile` передаётся из подписи в TXT-анализ и сохраняется при `/txt_resume`; владелец видит выбранный режим до AI POST.
- `src/features/audit/astraTextAttachment.js`: в режиме `wholeFile=true` один полный документ целиком внутри одного `userPrompt`; ни проактивный split, ни split после `context_length_exceeded` не выполняются; максимум один новый AI POST; ответ доставляется одним итоговым TXT.
- `src/features/audit/telegramTextJobControl.js`: сохранение режима с исходником и SHA-256, предотвращение смены режима между первичным запросом и `/txt_resume`, отдельные стадии и одна логическая часть в статусе.
- `tests/ai/v188134WholeFileMode.test.mjs`: проверка режима с различными моделями, полного текста >775 506 символов, отказа роутера, отсутствия повторных POST и сохранения режима.
- Исходное прерванное задание не изменяется автоматически и не повторяется новым POST.
