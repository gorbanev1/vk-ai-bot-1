V118 — FULL AI AUDIT + TELEGRAM HOME FAST-PATH

1. Owner command: «Гигорейв тест всех моделей».
2. Uses only current .env. Runtime parsed_secrets auto-import is disabled.
3. Startup deletes legacy parsed_secrets.txt / parsed_secrets.jsonl / parsed-secrets.txt without parsing them.
4. Live-audits every configured key catalog, every detected text/chat model, and every detected image-generation model.
5. Writes AI_FULL_AUDIT_RESULTS/<timestamp>/report.txt, report.json, results.jsonl, retry_candidates.json and sol_summary.txt.
6. Rebuilds SQLite ai_runtime_modes from successful key+model+capability combinations only.
7. Failure classes include auth, quota, server/network, model/endpoint, and payload/endpoint, each with a second-round retry strategy.
8. Final compact technical summary is requested from GPT-5.6 Sol through OPENAI_COMPAT_API_KEY when available.
9. Telegram /start, /menu, /home, «главная», «главное меню», «домой» and «🏠 На главную» bypass the application router and always restore the main keyboard.
10. Restoring an event in Telegram returns the owner main keyboard.

No .env or data/bot.sqlite is included in the patch/full release.
