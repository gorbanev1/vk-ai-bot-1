GIGORAVE PATCH V121 -> V123

1. Остановить `pnpm dev`.
2. Скопировать `.env` и `data/bot.sqlite` в безопасное место.
3. Распаковать patch в корень проекта с заменой файлов.
4. Не удалять `.env` и `data/`. Патч их не содержит.
5. `pnpm install` (если зависимости ещё не установлены).
6. `pnpm dev`.

Пинбол:
- добавь TELEGRAM_PINBALL_WEBAPP_URL=https://YOUR_DOMAIN/pinball/
- reverse proxy -> http://127.0.0.1:8787

AI audit:
- команда `Гигорейв тест всех моделей`
- owner GPT endpoint/GigaChat не трогаются
- definitive invalid чужие ключи очищаются из .env только после completed audit с backup.
