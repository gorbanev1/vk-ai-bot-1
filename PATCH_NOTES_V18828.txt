Gigorave 0.188.28
Build: events-v18828-source-integrity-poster-priority-db-repair

FIXES
- Non-destructive repeat parsing: an empty refresh never deletes previously accepted events.
- Startup strict event cleanup is audit-only; no destructive DELETE on boot.
- VK poster precedence: real wall/repost photo > exact DOM post media > link/document preview.
- Browser exact-post extraction rejects avatar/profile/logo/icon/badge UI images.
- Exact source reparse merges API + browser evidence instead of letting API preview-only override the real poster.
- Added REPAIR_EVENT_DB_FROM_BACKUPS.mjs for safe merge of missing future event/source rows from healthy backups.
- Keeps 6-hour healthy SQLite scheduler with 15 retained snapshots and V188.27 browser lifecycle.

DATA SAFETY
- Release archive must not contain data/, *.sqlite, *.db, *-wal, *-shm or runtime jsonl files.
