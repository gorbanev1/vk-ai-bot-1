EVENTS V19 — загрузка GPT-картинок во VK

Исправлено:
- готовый PNG передаётся в vk.upload.messagePhoto как Buffer с filename/contentType/contentLength;
- абсолютный путь к файлу больше не передаётся как source.value;
- добавлен резервный прямой цикл загрузки VK:
  photos.getMessagesUploadServer -> multipart photo -> photos.saveMessagesPhoto;
- attachment проверяется до messages.send;
- при полном сбое PNG сохраняется в data/generated-images;
- в логах видно, какой способ сработал: vk-io-buffer или direct-multipart.

Установка:
распаковать поверх проекта с заменой src/index.js и перезапустить процесс.
