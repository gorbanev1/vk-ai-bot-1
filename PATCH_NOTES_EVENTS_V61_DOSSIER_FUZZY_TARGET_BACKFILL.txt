V61 — dossier fuzzy target + on-demand backfill

Исправления:
- команда «досье» принимает имя, ник, похожую форму, @username, VK mention и id;
- локальный поиск использует единый fuzzy resolver и выбирает максимальное совпадение;
- для VK в alias участника добавляется screen_name;
- выбранная цель всегда переводится в числовой user_id до чтения SQLite;
- если dossier_facts пуст, после успешного пароля бот строит факты по всем собственным сообщениям цели в текущей беседе;
- on-demand досье сохраняется в SQLite и используется дальше без повторной полной сборки;
- чужие сообщения и упоминания не записываются как подтверждённые факты;
- добавлены регрессионные тесты;
- версия: 0.61.0, build: events-v61-dossier-fuzzy-target-backfill.

Новых зависимостей, переменных окружения и миграций базы нет.
