V86

- Добавлены обязательные VK-источники deadway36, liverpool_pub_vrn, idmamaanarchy, meetbowling.club, vavilone_rb.
- Каждый из них имеет minimum initialCount=10; старое значение <10 не уменьшает этот минимум.
- VK public scraper теперь открывает vk.ru, а не vk.com.
- Перед финальным чтением первых N постов скрейпер последовательно прокручивает каждый пост в viewport, чтобы VK подгрузил currentSrc/background-image.
- После прокрутки скрейпер ждёт lazy images/backgrounds и удерживает вкладку открытой ещё 8 секунд по умолчанию.
- Telegram HTML scraper больше не делает page.content() сразу после первого .tgme_widget_message. Он прокручивает страницу, ждёт lazy-media/background-image, удерживает вкладку открытой 10 секунд, затем фиксирует HTML и только после этого закрывает вкладку.
- Общий browserGrabber получил settleScraperPage(): network idle best-effort, scroll, preload CSS background-image, ожидание img.complete, configurable hold.
- Добавлены логи media-settle и количества захваченных постов.
- Добавлен pure test обязательного списка VK-источников.
