EVENTS V22 — ЖЁСТКАЯ МАРШРУТИЗАЦИЯ PRO2 / PRO3

Исправлено:
- pro3 и pro2 распознаются в любой позиции запроса, а не только в начале или после слова «прашна»;
- приоритет режимов жёсткий: pro3 > pro2 > pro;
- для прашны pro3 всегда выбирает gpt-5.6-sol и пакет full;
- для прашны pro2 всегда выбирает gpt-5.6-terra и пакет balanced;
- добавлен защитный инвариант: pro2/pro3 не могут уйти с пакетом fast;
- при запуске выводится [BOT PATCH] events-v22-strict-pro-routing, чтобы было видно, что новая версия реально запущена;
- добавлены тесты точной пользовательской команды и вариантов, где режим стоит в конце или внутри строки.
