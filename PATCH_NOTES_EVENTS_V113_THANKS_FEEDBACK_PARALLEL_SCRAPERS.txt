V113 — Peresozdanie thanks/feedback restore + parallel manual scrapers

Runtime fixes:
- Restored Telegram button: «🖤 Поблагодарить за Пересоздание» from 22.08.2026 20:00 Europe/Moscow onward.
- Restored the dedicated `пересоздание спасибо` branch: feedback text first, Sber QR second.
- Restored V109/V110 post-event feedback persistence and owner forwarding.
- Restored owner post-event broadcast draft/send/edit/cancel/template/reviews/stats/QR toggle commands.
- Successful post-event deliveries are armed for one feedback reply.
- V111 coords and V112 unique requester/usernames behavior remains intact.

Scraper behavior:
- `Гигорейв парсер все` now starts configured sources concurrently instead of waiting for each source to finish.
- Starts are staggered by SCRAPER_ALL_LAUNCH_STAGGER_MS (default 350 ms) to avoid navigation bursts.
- VK public pages scroll gradually with smooth scrollBy steps instead of jumping to document bottom.
- VK_PUBLIC_SCROLL_DELAY_MS controls the VK public step delay (default 1200 ms).
- Telegram render scrolling is slower and configurable via TELEGRAM_HTML_RENDER_SCROLL_DELAY_MS (default 900 ms).
- Shared browser settling uses smooth smaller viewport steps.

Release hygiene:
- Full archive restores package.json.
- Full archive intentionally excludes local secrets/backups, .git/.idea, and runtime data.
