V94 — глубокая fixed-point дедупликация афиши + AI merge деталей

1. Останови бота.
2. Распакуй V94 поверх текущего проекта V93.
3. НЕ удаляй и НЕ заменяй:
   .env
   data/
   SQLite БД
   browser profile
4. При необходимости:
   pnpm install --frozen-lockfile
5. Проверка нового алгоритма:
   pnpm run test:event-two-contour
   pnpm run test:event-moderation
   pnpm run test:event-normalization
6. Запуск:
   pnpm dev

Ожидаемая сборка:
[BOT BUILD] events-v94-deep-fixed-point-event-deduplication

После запуска проверь:
Гигорейв тусы август кратко

Что должно измениться:
- TATTOOMO 2026 не должен повторяться второй строкой;
- Мото Што-то! не должен повторяться второй строкой;
- если один ивент найден в Telegram и VK, остаётся одна карточка и перечисляются оба источника;
- многодневные события выводятся по дням: дата -> время -> площадка;
- если первый контур не уверен, pair идёт в GPT second contour;
- если duplicate уже подтверждён, отличающиеся описания/расписание дополнительно объединяются GPT, но при ошибке GPT остаётся deterministic fallback без дублей.
