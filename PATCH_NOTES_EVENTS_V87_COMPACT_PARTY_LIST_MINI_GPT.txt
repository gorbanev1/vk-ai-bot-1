V87

- Добавлен compact-режим общей афиши по ключам «кратко» / «коротко» / «краткий список» / «короткий список».
- Ключ compact удаляется только для определения периода, поэтому работают варианты и в начале, и в конце команды.
- В compact-режиме getPublicEventsForRange берёт до 500 событий, чтобы не отрезать полный список диапазона старым лимитом 50/100.
- Все найденные события сохраняются приложением; GPT не формирует состав списка и не может удалить событие.
- gpt-5.4-mini получает события батчами и возвращает только короткое summary 2–3 предложениями для каждого key.
- Если GPT не вернул конкретный key или произошла ошибка, используется локальный summary из очищенного анонса.
- Итог строится одним общим текстом: название, дата/время, место, участники, цена, билетная ссылка (только если реально найдена), 2–3 коротких предложения.
- В compact-режиме изображения не готовятся, не загружаются и не отправляются.
- Билетная ссылка берётся только из явных ticketUrl/ticketsUrl/ticketLink/bookingUrl либо URL рядом со словами «билет/купить/касса/бронь/регистрация» в исходном описании; VK/t.me source URL не выдаётся за билетный.
- Справка VK/TG обновлена примерами «тусы ... кратко/коротко».
- V86 scraper changes сохранены: пять обязательных VK-источников по 10 постов + ожидание lazy-media в VK/Telegram перед закрытием вкладки.
