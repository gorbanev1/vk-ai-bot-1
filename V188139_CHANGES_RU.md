# V188.139 — одностраничный структурный эталон VK для «парсер всё»

- При полном ручном парсинге публичной страницы — один исходный baseline после открытия страницы и ДО цикла прокрутки и чтения постов; VK-беседа — один baseline ДО unread down-sweep/history backfill.
- Для каждого источника отдельно: `vk-dom-structure-baselines/<source>/dom.raw.html`, `dom.structure.json`, `page-state.json`, `resources.json`, `manifest.json`, `PROMPT_FOR_DOM_AUDIT_RU.txt`.
- Полный исходный `document.documentElement.outerHTML` и SHA-256; структурный индекс всех DOM-элементов с атрибутами, прямыми текстовыми узлами, parentIndex и цепочками предков wall-ссылок. Кандидаты НЕ считаются проверенными постами.
- Время тихого DOM ограничено 4,5 сек, timeout фиксируется: нулевая гарантия полноты загрузки от одного MutationObserver.
- Старые снимки КАЖДОЙ итерации V188.138 и poster logs V188.137 сохранены; штатная прокрутка и алгоритм выделения постов/афиш не менялись.
- Добавлена полностью офлайн-проверка baseline по SHA-256 и формату; нет обращения к VK, профилю, AI.
